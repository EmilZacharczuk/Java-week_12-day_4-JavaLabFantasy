package game.enums;

public enum SpellType {
        FIREBALL(10),
        LIGHTNINGSTRIKE(5;

        private final int points;

        SpellType(int points) {
            this.points = points;
    }
}
